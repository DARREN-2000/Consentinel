apiVersion: v1
kind: Service
metadata:
  name: {{ include "consentinel.fullname" . }}
  labels:
    {{- include "consentinel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "consentinel.selectorLabels" . | nindent 4 }}
